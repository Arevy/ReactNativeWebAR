import * as React from 'react';
import { Text, View } from 'react-native';

export const App: React.FC = () => {
  return (
    <View>
      <Text>Salut din React Native Web</Text>
    </View>
  );
}

export default App;
